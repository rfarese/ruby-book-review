<h3><%= @book.title %></h3>

<div class="row rank-form-container">

  <div class="large-4 columns">
    <h2>Create a Rank</h2>
    <%= render 'form' %>
  </div>

  <div class="large-4 columns">
    <%= image_tag @book.cover_photo.book_show.url, class: "book-show" if @book.cover_photo? %>
  </div>

  <div class="large-4 columns"></div>
</div>

<%= link_to "Back", book_path(@book) %>
