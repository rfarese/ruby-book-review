<% if is_admin? %>

<h1>All System Users</h1>
  <%= @users.each do |user| %>
    <h3><%= user.first_name %> <%= user.last_name %><h3>
    <ul>
      <li>Email: <%= user.email %></li>
    </ul>
  <% end %>
<% end %>
