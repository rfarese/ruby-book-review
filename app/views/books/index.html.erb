<h1 class="text-center">Ruby Books</h1>

<% @books.each do |book| %>
  <a href="/books/<%= book.id %>">
    <%= image_tag book.cover_photo.book_index.url, class: 'book' %>
  </a>
<% end %>

<%= paginate @books %>
